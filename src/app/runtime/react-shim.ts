import React from "react";

export const MODULE_URL = import.meta.url;

export default React;
export const {
  Fragment,
  createElement,
  useCallback,
  useContext,
  useEffect,
  useMemo,
  useReducer,
  useRef,
  useState,
} = React;
